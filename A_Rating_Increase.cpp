#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define fastio()                      \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);                    \
    cout.tie(NULL);

void solver()
{
    string s;
    cin >> s;
    int n = s.size();
    vector<int> a(n);
    for(int i=0; i<n; i++)
    {
        a[i]=s[i];
    }
    string x,y;
    x="";
    y="";
    for(int i=0; i<n/2; i++)
}

int main()
{
	fastio();
	return 0;
}